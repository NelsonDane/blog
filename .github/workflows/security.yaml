name: Workflow Security
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/*

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  security:
    permissions:
      security-events: write # Write to GitHub Security Events
      contents: read # Read contents
      actions: read # Read external action
    name: Workflow Security
    uses: NelsonDane/Custom-GitHub-Actions/.github/workflows/security.yaml@b1f8f69f32ea50a482e2eac5da4af4013ac6c6ee
